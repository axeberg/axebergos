tee(1)                      General Commands Manual                     tee(1)

NAME
       tee - duplicate standard input

SYNOPSIS
       tee [-a] FILE

DESCRIPTION
       Copy standard input to both standard output and FILE.

OPTIONS
       -a
           Append to file instead of overwriting.

EXAMPLES
       Save and display:

           echo hello | tee output.txt

       Append to file:

           echo more | tee -a output.txt

       Save intermediate pipeline output:

           cat data | grep pattern | tee matches.txt | wc -l

SEE ALSO
       cat(1)

                                  2025-12-24                            tee(1)
